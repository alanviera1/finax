// Este archivo es una plantilla. Copialo como `secrets.js` y coloca tus llaves
// reales. `secrets.js` NO se versiona en git (ver .gitignore); el archivo real
// debe existir en la maquina desde la que se ejecute `firebase serve` /
// `firebase deploy`, o la app no cargara.

const GEMINI_API_KEY = "TU_API_KEY_DE_GEMINI_AQUI";

export { GEMINI_API_KEY };
